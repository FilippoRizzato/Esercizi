#include <stdio.h>
int main(){
    int n;
    int menon;
    do{
        printf("Inserisci n \n");
    scanf("%d", &n);

    }while(n < 0);
    for (int i = 0; i < n; i++)
    {
       menon = -1 *n;
       i = menon;
       printf(" Il numero <= è: %d\n", i);
    }
    
    return 0;
}

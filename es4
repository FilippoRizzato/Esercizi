#include <stdio.h>
#include <math.h>
 
int main()
{
  int num1;
        int num2;
        double risultato;
    do{
 
       
        printf("inserisci il 1° numero\n");
        printf("Inserisci il 2° numero \n");
        scanf("%d", &num1);
        scanf("%d", &num2);
 
        if(num2 < num1)
        {
            risultato = (double)num2/(double)num1;
 
        }
        else{
            risultato = (double)num1/(double)num2;
        }
        if(risultato > 0){
            risultato = sqrt(r);
            printf("la radice sarà %f\n\n", risultato);

        }
 
 
    }while(risultato > 0);
    printf("il numero è minire di 0 non esiste la radice di un numero negativo:(\n\n");
    return 0;
    }
